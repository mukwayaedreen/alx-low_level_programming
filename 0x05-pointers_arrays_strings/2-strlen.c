#include "main.h"
#include <string.h>

/**
* _strlen - function which returns the length of string
* @s: pointer holding the string
* Return: always 0
*/

int _strlen(char *s)
{
	int a;

	a = strlen(*s);
	return (a);
}
