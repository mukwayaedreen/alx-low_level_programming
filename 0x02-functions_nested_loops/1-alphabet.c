#include "main.h"

/**
 * main - prints alphabet in lowercase
 * Return: Always 0 (success)
 */

int print_alphabet(void)
{
	int j;
	
	for (j = 'a'; j <= 'z'; j++)
	{
		_putchar(j);
	}
	_putchar(j);

	return (0);
}
