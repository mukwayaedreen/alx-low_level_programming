#include "main.h"

/**
* print_last_digit -> print the last digit of a number
* @n: holds the last digit
* Return: last digit
*/

int print_last_digit(int n)
{
	int digit;

	digit = n % 10;

	return (digit);
}
