#include <stdio.h>

/**
  * main - Prints the lowercase alphabet in reverse.
   *
   * Return: Always 0.
   */

int main(void)
{

char mk;

for (mk = 'z'; mk >= 'a'; mk--)

putchar(mk);

putchar('\n');

return (0);
}
