#include <stdio.h>

/**
 * main - Entry point
 * description: prints lowercase and uppercase alphabet
 * Return: Always 0 (success)
 */

int main(void)
{

intt ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(c);
}
for (ch = 'A'; ch <= 'Z'; ch++)
{
putchar(x);
}
putchar('\n');

return (0);
}
